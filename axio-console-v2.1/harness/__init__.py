"""AXIO multi-agent harness (Plan Fase C).

Orchestrator, dispatcher, fallback engine, task graph, event bus, monitor and
synthesizer — the plan-level machinery that routes each step to the right agent,
runs independent steps in parallel, recovers via fallback, and merges results.
"""
