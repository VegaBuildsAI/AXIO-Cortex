# AXIO — Current State
> Live status for coding-assistant sessions. Narrative zone is human/`/update-context`-maintained; the AUTO-SNAPSHOT zone is regenerated by the SessionEnd hook. Durable architecture lives in `CLAUDE.md`; deep memory internals in `docs/CORTEX.md`.

## Current focus
- Platform is stable at **v2.1 "Continuous Resilient Cortex Memory"**. Code mode is the unified 38-tool agent; memory is the resilient Postgres Cortex + continuous self-memory.

## Recently done (this line of work)
- **Web-evidence gate** — `core/web_intent.py` (intent detection + `augment_with_web`); wired into Code via `_completion_gate()` in `modes/code.py`; Chat/Cowork now do web-augmentation before answering current-info questions.
- **GitHub toolset** — `core/code_tools/github_tools.py` (branch/commit/push/fork/PR via `gh` CLI + git); push/fork/PR-create/merge are `external` (approval-gated).
- Registry is **42 tools** (categorized, with a full/dynamic tool router `CODE_TOOL_ROUTING_MODE`): the GitHub toolset plus Codex's artifact tools (`create_docx/xlsx/pptx/pdf`). Docs/tests reconciled to 42.
- **Skill curation** — 8 new `skills/axio-coding/references/*.md` (filesystem, git, web, browser, retrieval, scaffold, verification-and-recovery, github-workflow) + tool→reference index in `SKILL.md`.
- **Consolidation** — canonical root is `C:\Users\AXIO\AXIO Model Improvement\axio-console-v2.1`; the old `C:\Users\AXIO\axio-console` line is archived (`axio-console_ARCHIVED_gemma-line`). Docs corrected to the Root 3 path and 38-tool count.

## In flight
- `AXIO_HARNESS_INTEGRATION.md` — Cortex harness integration (plan-stage).
- Continuous self-memory hardening (`CODEX_SELF_MEMORY_HANDOFF.md`).
- **Startup-context system** (this kit): global `~/.claude/CLAUDE.md`, per-project `CLAUDE.md`+`docs/STATE.md`, SessionEnd snapshot hook, Second Brain MCP as standing secondary context.

## Do NOT touch
- `core/config.py` = single source of truth · `rev_agent.py` system prompt (monkey-patched by `modes/revrec.py`) · `modes/gemma_code.py` shim (used by `tests/test_coding_skills.py`) · `MEMORY_EMBED_MODEL` (768-dim vector store).

## Gotchas
- **Memory backend default is `json`** (`config.py:166`); the live Postgres Cortex is active only when `AXIO_MEMORY_BACKEND=postgres` (set by the `.cmd`/scheduled tasks, not ad-hoc CLI runs).
- Cortex **schema lives outside the repo**: `C:\Users\AXIO\AXIO Model Improvement\docker-compose.yml` + `docker/postgres/init/*.sql` (a fresh clone won't provision the DB).
- Web features need SearXNG (`AXIO_SEARXNG_URL`, default `127.0.0.1:8080`) up; GitHub tools need `gh auth login` (authed as `VegaBuildsAI`).
- `pytest` is now in `requirements.txt`; tests are `unittest`-based (`py -m pytest tests -q` or `py -m unittest discover -s tests`).

## Doc currency map (don't trust the stale ones)
- **Current:** `CLAUDE.md`, `AGENTS.md`, `docs/STATE.md`, `docs/CORTEX.md`, `CLAUDE_CODE_CORTEX_MEMORY_AUDIT_HANDOFF.md`, `CODEX_SELF_MEMORY_HANDOFF.md`; seeds `AXIO_CORTEX_MEMORY.md`, `AXIO_AI_WORKFLOW.md`.
- **Stale/superseded:** `CHANGES.md`, `FIXES_SUMMARY.md`, `IMPROVEMENTS.md`, `UI_README.md` (model drift), `AXIO_GEMMA_PLAN.md` (self-labeled superseded). (`docs/ARCHITECTURE.md` is now current — diagram + prose reflect the Postgres-Cortex + gemma4/Claude reality.)

<!-- AUTO-SNAPSHOT:BEGIN -->
### Snapshot (auto)
- Updated: 2026-08-26 23:56 UTC
- Branch: `codex/self-memory-rag`
- Recent commits:
  - 1c6043c Add unified IOAF architecture diagram + docs
  - 3344b16 Add continue-nudge to Code agent; generalize Cortex memory seed
  - e9c453b Merge pull request #2 from VegaBuildsAI/feat/cortex-memory-seed
  - 51abee2 Seed AXIO Cortex living memory: identity + platform knowledge
  - 54c5139 Merge pull request #1 from VegaBuildsAI/fix/cortex-live-memory-p1
- Uncommitted (38 files changed, 2802 insertions(+), 1898 deletions(-)):
  M .env.example
   M .gitignore
   M AGENTS.md
   M AUDIT_PROMPT.md
   M AXIO_CORTEX_MEMORY.md
   M CLAUDE.md
   M axio.py
   M config/models.yaml
   M config/routing.yaml
   M core/config.py
   M core/file_context.py
   M core/logger.py
   M core/memory.py
   M core/memory_backends/postgres_backend.py
   M core/mode_memory.py
  … (+83 more)
<!-- AUTO-SNAPSHOT:END -->
